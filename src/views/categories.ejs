<%- include('partials/header') %>

    <main>
        <h1><%= title %></h1>

        <p class="subtitle">Explore our service projects categories:</p>

        <ul>
            <% categories.forEach(category => { %>
                <li><strong><%= category.name %></strong></li>
            <% }) %>
        </ul
        
    </main>

<%- include('partials/footer') %>